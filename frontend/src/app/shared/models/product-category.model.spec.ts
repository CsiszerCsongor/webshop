import { ProductCategory.Model } from './product-category.model';

describe('ProductCategory.Model', () => {
  it('should create an instance', () => {
    expect(new ProductCategory.Model()).toBeTruthy();
  });
});
