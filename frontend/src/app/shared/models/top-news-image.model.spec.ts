import { TopNewsImage.Model } from './top-news-image.model';

describe('TopNewsImage.Model', () => {
  it('should create an instance', () => {
    expect(new TopNewsImage.Model()).toBeTruthy();
  });
});
